#Geocoding Postcodes using google

#Please note google's terms of service 
#and policy with regards of the number 
#of times you can geocode in a day etc

#To use this function simply open the 
#geocode.php file & enter your api maps 
#key in the $config['maps_key'] variable

#Get the latitude & longitude like so
#$postcode = "EH12 5UB";
#$coords = get_lat_long($postcode);
#$coords['lat']; 	//latitude
#$coords['lng'];	//longitude

#uses google.co.uk by default

#International Geocoding
#add the domain of google that you wish
#to geocode from i.e. Canada
#$postcode = "T0A 0A0";
#$coords = get_lat_long($postcode, "ca");
#$coords['lat']....
#will use google.ca to geocode